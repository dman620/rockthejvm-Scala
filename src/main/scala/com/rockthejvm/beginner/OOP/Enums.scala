package com.rockthejvm.beginner.OOP

object Enums extends App {
  //enums are only supported in Scala 3
  //therefore I will record the syntax here, since this is a scals 2 project

  /*enum Permissions{
  *   case READ, WRITE, EXECUTE, NONE
  *   def openDocument(): Unit =
  *     if(this == READ) println("opening...")
  *     else println("reading not allowed.")
  * }
  * val somePermissions: Permissions = Permissions.EXECUTE
  *
  *
  * */
}
